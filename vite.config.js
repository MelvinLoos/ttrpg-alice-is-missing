import { defineConfig } from 'vite'

// When deploying to GitHub Pages under a repository subpath,
// set `base` so built assets reference the correct subfolder.
export default defineConfig({
  base: '/ttrpg-alice-is-missing/',
})
